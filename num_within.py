#This function will take a number and a range (inclusive) as input and return True if the number falls within the given range, otherwise False.
def num_within(num, start, end):
    return start <= num <= end
